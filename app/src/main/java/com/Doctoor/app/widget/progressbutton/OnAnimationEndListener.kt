package com.Doctoor.app.widget.progressbutton

/**
 *
 * Interface that can be implemented if the activity needs to start a event when the animation of the
 * button finishes.
 */
interface OnAnimationEndListener {
    fun onAnimationEnd()
}
